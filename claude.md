

**ROLE & CONTEXT**
Act as an Expert Full-Stack Developer specializing in Laravel and Vue.js.
We are going to build a Recipe and Pantry Management web application.

**CRUCIAL RULES FOR THIS SESSION (ZERO TOLERANCE FOR DEVIATION)**
**STRICT INCREMENTALITY (DISCUSS FIRST, CODE LATER):** Do NOT, under any circumstances, try to build the whole app or combine steps. I will stop you if you rush ahead. We are taking this one granular step at a time. For every single step below, you must:

* **Phase A (Analysis):** Tell me your plan for the specific step. What files will you touch? What is the database schema? What is the architectural approach? Stop and wait for my explicit approval.
* **Phase B (Coding & Verification):** ONLY after I approve your plan, give me the code for that step. **Extra Step:** Write a simple test, visualization, or demo for each step as it happens so that it can clearly be seen the last step was done correctly. Then, wait.
* **Phase C (Version Control): Conjure up a single git add . and git commit -m "..." command with a comprehensive, realistic commit message summarizing the entirely completed step. Do NOT attempt to split the code into multiple commits, as this causes context fragmentation. Output exactly one commit command. Then, ask for permission to move to Phase A of the next step.

**CODE STYLE & QUALITY:**

* **No Emojis:** Absolutely zero emojis in the code, terminal commands, or comments.
* **Minimal Comments:** Do not write an absurd amount of comments. Keep comments strictly pragmatic and concise.

**ARCHITECTURE & TECH STACK (Simpler Approach):**
We will use Laravel Breeze with Inertia.js and Vue 3. This avoids the complexity of a decoupled SPA and Vue Router. We will pass data directly from controllers to Vue components via Inertia props, formatted using Laravel API Resources.

* **State Management Strategy:** Use Inertia.js as the primary source of truth for data. Use Pinia strictly for transient UI state or complex client-side calculations (e.g., real-time filtering logic or temporary shopping list toggles). Ensure you properly hydrate/sync the Pinia state when Inertia makes server requests to prevent data mismatches.
* **File Uploads:** Handle multi-image file uploads natively using Laravel's public disk via the Storage facade.

**STRICT MVC COMPLIANCE:**

* **Skinny Controllers, Fat Models:** Controllers should only handle HTTP requests and responses. Push business logic into Models or dedicated Service classes.
* **Separation of Concerns:** Use Laravel Form Requests for validation. Vue.js is strictly for the View layer (UI and state)—do not leak backend business logic into the frontend.
* **Database First:** Always finalize your database structure (Migrations) before writing models or logic.

**APP LANGUAGE:**
The application's UI, frontend text, database default seeders, and user-facing messages MUST BE ENTIRELY IN BULGARIAN. You can write your code comments and variable names in English.

**LIGHTWEIGHT TESTING:**
Keep testing minimal and pragmatic. A single happy-path feature test for the core search algorithm later on is sufficient. Focus your energy on architecture and clean code.

**USER ROLES**

* **Обикновен потребител (Normal User):** Can create/manage their own recipes, use the virtual pantry, and generate shopping lists.
* **Администратор (Admin):** Has overarching control. Can view, edit, and delete any recipe. Can manage user accounts.

**PROJECT EXECUTION PLAN (Read only, wait for my command)**

**STEP 0:** ENSURE ALL NEEDED LARAVEL AND OTHER REQUIREMENTS ARE PRESENT.

**STEP 1: Scaffolding & Authentication**
Initialize the Laravel project using Laravel Breeze (Vue/Inertia stack). Implement the user role system using a simple is_admin boolean on the User model.
    **STEP 1, PHASE A: Scaffolding & Authentication (Analysis & Plan)**
    1. Architectural Approach (Scaffolding)
    We will initialize the project using Laravel Breeze configured with the Vue/Inertia stack. This provides our foundational routing, Vite configuration, Tailwind CSS, and standard session-based authentication out of the box.
    2. Database Schema (The "Database First" Rule)
    Before running any migrations, we will intercept the default Laravel 0001_01_01_000000_create_users_table.php migration.
    We will add a single column: `$table->boolean('is_admin')->default(false);`.
    Security Note: Defaulting to false ensures anyone registering via the public frontend is automatically a standard user (Обикновен потребител).
    3. Model & Business Logic
    File: `app/Models/User.php`.
    We will add is_admin to the `$casts` array as a boolean.
    Security Note: We will specifically exclude is_admin from the `$fillable` array. This prevents mass-assignment vulnerabilities where a malicious user could pass is_admin: true during standard registration.
    4. Localization (Bulgarian UI Rule)
    We will modify the default English Vue components generated by Breeze (Login.vue, Register.vue, and the navigation in AuthenticatedLayout.vue).
    All user-facing text (labels, buttons, links) will be translated to Bulgarian (e.g., "Email" -> "Имейл", "Log in" -> "Вход").
    5. Database Seeding
    File: `database/seeders/DatabaseSeeder.php`.
    To enable immediate testing of our role system in future steps, we will write a seeder that creates two specific accounts using Bulgarian names:
    One "Администратор" (is_admin = true)
    One "Потребител" (is_admin = false)

**STEP 2: Database Schema & Models (Database First)**
Create migrations for recipes, ingredients, and the pivot table ingredient_recipe.
Polymorphic Images: Instead of a dedicated recipe images table, implement a polymorphic images table (imageable_id, imageable_type, path) to allow for future extensibility (e.g., ingredient or profile photos).
Pantry & Units: Create a pantry_items table. Establish a "just decent" unit conversion strategy using standard metric scaling (grams to kilograms, etc.). Ensure base units are saved to the DB for algorithm accuracy. Create Eloquent Models and Seeders with realistic Bulgarian data.
    **STEP 2, PHASE A: Database Schema & Models (Comprehensive Plan)**
    1. Architectural Strategy
    We are building a highly normalized database optimized for the "Какво да сготвя?" algorithm. The core strategy relies on isolating "display units" (what the user sees) from "base units" (what the engine calculates). We will enforce strict data integrity using database-level cascading deletes.
    2. Core Database Schema (Migrations)
    `ingredient_categories`
    id, name (String: e.g., "Млечни", "Месо")
    Purpose: Crucial for grouping items neatly in the Step 8 Virtual Pantry UI.
    `ingredients` (The Master Dictionary)
    id
    ingredient_category_id (Foreign Key -> restricted delete)
    name (String, unique: e.g., "Брашно")
    base_unit (String: strictly 'g', 'ml', or 'бр.')
    timestamps
    `recipes`
    id
    user_id (Foreign Key -> cascade delete)
    title, description, instructions (Strings/Text)
    prep_time, cook_time, servings (Integers)
    timestamps
    `ingredient_recipe` (Pivot Table - The Engine's Fuel)
    id (Primary key for easier API updates)
    recipe_id (Foreign Key -> cascade delete)
    ingredient_id (Foreign Key -> cascade delete)
    display_quantity (Decimal: e.g., 1.5)
    display_unit (String: e.g., 'kg')
    normalized_quantity (Decimal: e.g., 1500 - INDEXED for fast mathematical comparisons during the search algorithm)
    `pantry_items` (The Virtual Pantry)
    id
    user_id (Foreign Key -> cascade delete)
    ingredient_id (Foreign Key -> cascade delete)
    quantity (Decimal: explicitly stored in base_unit - INDEXED)
    `shopping_list_items` (The Robust Solution)
    id
    user_id (Foreign Key -> cascade delete)
    ingredient_id (Foreign Key -> cascade delete)
    quantity (Decimal: stored in base_unit)
    is_purchased (Boolean: default false)
    timestamps
    Purpose: Persistent, interactive shopping list tied directly to the master ingredient dictionary.
    `images` (Polymorphic)
    id
    path (String: storage path)
    imageable_id (Unsigned Big Integer)
    imageable_type (String)
    timestamps
    3. Eloquent Models & Relationships
    User: hasMany Recipes, hasMany PantryItems, hasMany ShoppingListItems.
    IngredientCategory: hasMany Ingredients.
    Ingredient: belongsTo Category, belongsToMany Recipes, hasMany PantryItems, hasMany ShoppingListItems.
    Recipe: belongsTo User, belongsToMany Ingredients (with pivot columns), morphMany Images.
    PantryItem & ShoppingListItem: belongsTo User, belongsTo Ingredient.
    4. Unit Conversion & Backend Logic
    We will create a backend configuration array or service class (UnitConverter) to handle standard metric scaling before data is saved to the pivot table:
    Weight: kg -> g (* 1000)
    Volume: l -> ml (* 1000), супена лъжица -> ml (15), чаена лъжица -> ml (5)
    Pieces: бр. remains бр.
    5. Database Seeders (Bulgarian Locale)
    To facilitate immediate UI development, I will write seeders generating realistic Bulgarian data:
    Pre-populate 5 ingredient_categories and ~15 standard ingredients.
    Create 2-3 standard Bulgarian recipes (e.g., "Мусака") linked to our test users, complete with pivot data and dummy polymorphic image paths.
    Pre-fill a pantry_items list for the test user to enable immediate testing of the "Partial Match" algorithm later.

**STEP 3: Backend Logic for Recipe CRUD & Permissions**
Build Form Requests for validation and API Resources for Inertia. Build Controllers with logic to store uploaded images. Crucial: Remember that Inertia requires a _method: 'PUT' spoof when sending Multipart/Form-Data in a Laravel update request. Set up Laravel Policies to enforce role-based access.
Create the backend architecture for Recipe CRUD. Prioritize performance (Eager Loading), decoupled logic, and strict file-system/database synchronization.
    **STEP 3, PHASE A: Backend Logic (Analysis & Plan)**
    1. Architectural Approach (Strict Decoupling)
    Create a RecipeController and a RecipeService.
    Decoupling Rule: The Controller MUST NOT pass the `$request` object into the Service. It must only pass `$request->validated()`.
    2. Database Performance (Eager Loading - CRUCIAL)
    To prevent N+1 query bottlenecks, whenever the RecipeController fetches recipes for the RecipeResource, it MUST explicitly eager load relationships: `Recipe::with(['ingredients', 'images'])`.
    3. Authorization & Validation
    Create a RecipePolicy (is_admin bypasses all via before, normal users can manage only their own).
    Create StoreRecipeRequest and UpdateRecipeRequest.
    Array Validation: Deeply validate the ingredients array. Validate the images array using wildcards (`images.*` max 2MB).
    Update Validation (The "Keep" List): For updates, validate a `keep_images` array (containing IDs of existing images the user wants to keep).
    4. Data Formatting (API Resources)
    Create RecipeResource and IngredientResource to strictly control the JSON payload sent to Inertia Vue. Flatten pivot data and polymorphic image URLs for clean frontend consumption.
    5. The "No Orphans" File Handling Strategy (Creation)
    Inside RecipeService, wrap the recipe creation, pivot syncing, and image uploading in a `DB::transaction()` inside a try/catch block.
    If an exception is thrown (the catch block), you MUST physically delete any newly uploaded images from `Storage::disk('public')` before re-throwing the error to prevent orphaned files.
    6. Bulletproof Image Updates (Deletions + Additions)
    During an update, the frontend will send new files AND a `keep_images` array of IDs.
    Inside the transaction: Delete the database records for any existing images NOT in the `keep_images` array (these are "stale"). Upload the new images and save their records.
    Physical Deletion Rule: Do NOT delete the physical files for the stale images inside the transaction. Wait until the transaction successfully completes. Delete the physical stale files ONLY after the try block succeeds.
    Inertia Spoofing Rule: Explicitly handle the Inertia `_method: 'PUT'` quirk for multipart/form-data.
    7. Pivot Table Normalization
    Before saving to the `ingredient_recipe` pivot table, calculate the `normalized_quantity` based on base units (e.g., 1.5 kg -> 1500).

**STEP 4: Frontend UI - Recipe Catalog**
Build the "Каталог на рецептите" (Recipe Catalog) Inertia Vue page. Ensure the UI extracts and displays the first associated polymorphic image as a "cover photo."
Build the "Каталог на рецептите" (Recipe Catalog) page AND update the global layout to strictly match the "style" wireframe.
CRUCIAL DIRECTIVE: Extract complex CSS art (notebook lines, gradients, shadows) into `resources/css/app.css` using `@layer` components. Do not clutter Vue templates with 30+ utility classes per div.
    **STEP 4, PHASE A: Frontend UI (Analysis & Plan)**
    1. Tailwind Config & The Scrapbook Aesthetic (Strict CSS Translation)
    Update `tailwind.config.js` with the wireframe's colors (bg-beige, cherry-red, etc.) and fonts (caveat, lora, montserrat).
    In `app.css`, you MUST translate these specific wireframe details into CSS classes:
    - The Background: The specific gingham tablecloth gradient on the body.
    - The Paper Noise: The base64 SVG `<filter>` applied to the navigation bar.
    - The Angles: The specific CSS transforms (`rotate(-2deg)`, `rotate(2deg)`) used on the logo, polaroids, and sticky notes to give the organic "scrapbook" feel.
    - The Accents: The `.tape` (with backdrop-blur), the `.pin` (with inset shadow), and the retro `.btn-add` click effect.
    2. Global Layout (AuthenticatedLayout.vue)
    Update the Breeze layout wrapper. Apply the gingham background here.
    Build the custom Navigation bar with the paper texture and rotated logo.
    Build the `.journal-container` grid. Place the main slot (`<slot />`) on the left, and build the right-hand `.sidebar` (containing the "Моят Килер" box and the ".sticky-note" shopping list) so it is visible on all pages.
    3. Component Architecture (Decoupled & Reusable)
    `Pages/Recipes/Index.vue`: The notebook page itself (with the repeating-linear-gradient lines and the red double border).
    `Components/RecipePolaroid.vue`: The reusable card for recipes with images.
    `Components/RecipeRow.vue`: The minimal row format for the "latest-recipes" list.
    4. Performance & CLS Prevention (Optimal Performance Rule)
    Inside RecipePolaroid.vue, use Tailwind's aspect-ratio utilities on the image container to prevent Cumulative Layout Shift (CLS). Use `loading="lazy"`.
    5. Data Consumption & Graceful Fallbacks
    Index.vue receives paginated data via `defineProps({ recipes: Object })`.
    Fallback Engine: If `recipe.images` is empty, you MUST render RecipeRow.vue instead of RecipePolaroid.vue so the grid doesn't break with empty gray boxes.
    6. Localization & Empty States (Bulgarian Only)
    If `recipes.data.length === 0`, show a friendly handwritten note (using the Caveat font) saying: "Тетрадката е празна. Добави първата рецепта!"

**STEP 5: Frontend UI & Backend - Live Search**
Implement a live search bar (title/ingredient). Use a debounced Inertia `router.get` request with `{ preserveState: true, replace: true }`. Handle filtering via Eloquent when and orWhereHas in the controller.


STEP 5, PHASE A: Basic Search (Analysis & Plan)
    1. Backend (Laravel Controller) - Strict Implementation Rules:
    *   Target File: app/Http/Controllers/RecipeController.php (inside the index method).
    *   Negative Constraint: Do NOT create a custom Eloquent Local Scope. Write the query directly in the controller method.
    *   Base Query & Eager Loading: You MUST initialize the query using exactly $query = Recipe::with(['ingredients', 'images']); to prevent N+1 issues.
    *   Wildcard Rule (CRITICAL): Use an if ($request->filled('search')) block. Inside it, extract and format the search term immediately: $searchTerm = '%' . $request->search . '%';. This guarantees partial matches (e.g., searching "мусака" finds "вкусна мусака").
    *   The Eloquent Query: Chain the following exactly to your $query inside the if-block:
        ->where('title', 'like', $searchTerm)
        ->orWhereHas('ingredients', function($q) use ($searchTerm) { $q->where('name', 'like', $searchTerm); })
    *   Inertia Response: When returning the Inertia::render, you MUST pass the search term back to the frontend so it survives page reloads. Pass it exactly as: 'filters' => $request->only('search').

2. Frontend (Vue 3 + Inertia) - Strict Implementation Rules:
    *   Target File: resources/js/Pages/Recipes/Index.vue.
    *   State Hydration: Define the incoming props: const props = defineProps({ recipes: Object, filters: Object });. Initialize the search input's Vue ref using exactly: const search = ref(props.filters?.search || '');.
    *   Negative Constraint: Do NOT use Lodash debounce or Vue watch functions.
    *   Explicit Trigger: Bind the search input to a function that ONLY fires on @keyup.enter.
    *   Inertia Request: The search function MUST execute exactly this Inertia call:
        router.get(route('recipes.index'), { search: search.value }, { preserveState: true, replace: true });

3. Database Schema:
    *   No new migrations needed. Rely entirely on the existing schema from Step 2.

4. Localization (Bulgarian UI Rule):
    *   The search input placeholder must be strictly in Bulgarian: placeholder="Търси рецепта или съставка..."


**STEP 6: Frontend UI - Complex Recipe Form**
Build the "Добавяне на рецепта" Vue component. Use dynamic inputs for N ingredients. Use the Inertia form helper to handle multipart data, including the _method spoofing for edits, and basic client-side image previews.

STEP 6, PHASE A: Complex Recipe Form (Analysis & Plan)

1. Architectural Approach (Inertia Form Helper)
Target Files: resources/js/Pages/Recipes/Create.vue and resources/js/Pages/Recipes/Edit.vue (or a shared RecipeForm.vue component to keep code DRY).
We will use Inertia's useForm helper. This is the industry standard for Inertia apps, as it automatically handles loading states, validation errors, and progress bars without extra boilerplate.

2. Dynamic Ingredient Inputs (Minimal Complexity)
The `useForm` state will include an `ingredients` array.
We will create standard Vue methods (`addIngredient`, `removeIngredient`) that simply `push()` or `splice()` objects in this array, fulfilling the requirement for dynamic inputs for N ingredients. We will not use complex external drag-and-drop libraries to keep the code readable and maintainable for a university level.

3. File Uploads, Previews & Spoofing (Strict Implementation)
* **Previews:** We will use native HTML `<input type="file" multiple>` and standard JavaScript (`URL.createObjectURL()`) to generate basic client-side image previews before upload.
* **Edits & Method Spoofing:** When submitting the edit form with images, we will explicitly append `_method: 'PUT'` to the form payload. This is the required Inertia spoofing technique to route multipart/form-data to Laravel's update controller method.
* **The "Keep" List:** For the edit view, the form will also manage the `keep_images` array (as defined in Step 3's backend logic) so the user can delete specific existing images[cite: 67].

4. UI & Localization (Bulgarian Strictly)
All form labels, placeholders, and buttons will be strictly in Bulgarian.
Examples: "Заглавие на рецепта", "Време за приготвяне (мин)", "Добави съставка", "Запази рецептата".

5. Validation Feedback
We will bind the backend validation errors directly to the input fields using Inertia's built-in `form.errors` object, ensuring a smooth, SPA-like user experience without complex state management.

**STEP 7: The Engine - Pantry Algorithm (Backend Only)**
Write the backend logic in a dedicated Service class for "Какво да сготвя?".
Query 1 (Zero-Missing): 100% ingredient match.
Query 2 (Partial Match): Sort by ascending missing items. Write a basic happy-path test for this algorithm.


STEP 7, PHASE A: Pantry Algorithm (Analysis & Plan)

Architectural Approach (Service Layer Pattern)

Target File: app/Services/PantryAlgorithmService.php.

Strict Decoupling: Enforce the "Skinny Controllers" rule. The controller will only pass the authenticated User model to this service. The service handles the complex database queries and returns categorized collections of recipes.

Query Logic: Query 1 (Zero-Missing / Perfect Match)

Goal: Find recipes where the user has every required ingredient in their pantry in the correct quantities.

Execution: Use Eloquent's whereDoesntHave relationship method combined with a subquery. We will query recipes where the recipe does not have any ingredients that are missing or insufficient in the user's pantry_items table.

CRUCIAL ALGORITHM RULE (Extra Items): The matching logic must strictly be one-way. If a recipe requires Milk and Flour, and the user has Milk, Flour, and Beans in their pantry, this is still a Perfect Match. Do not compare total ingredient counts; only verify that the recipe has zero missing requirements. Ensure you check that the pantry quantity is >= the pivot's normalized_quantity.

Query Logic: Query 2 (Partial Match)

Goal: Find recipes where the user is missing just a few items, prioritizing the closest matches.

Execution: Use withCount combined with a constrained subquery to dynamically calculate a missing_ingredients_count attribute on the Recipe model.

Filters & Sorting: Filter out the Perfect Matches (to avoid duplicates in the UI). Filter out recipes missing too many items (cap it at a maximum of 3 missing items to keep results relevant and performant). Finally, sort the results using orderBy('missing_ingredients_count', 'asc').

Minimal Pragmatic Testing (The "Happy Path")

Target File: tests/Feature/PantryAlgorithmTest.php.

Strategy: Write one clean feature test using Laravel's RefreshDatabase trait.

The Scenario: Seed a test user. Add "Брашно" (Flour), "Яйца" (Eggs), and "Ориз" (Rice - acting as an extra, unneeded item) to their pantry. Create "Рецепта 1" requiring only Flour and Eggs. Create "Рецепта 2" requiring Flour, Eggs, and "Мляко" (Milk).

The Assertion: Assert that the PantryAlgorithmService places "Рецепта 1" in the Zero-Missing array (proving extra pantry items do not break the perfect match logic), and "Рецепта 2" in the Partial Match array with exactly 1 missing item.


**STEP 8: The Engine - Virtual Pantry (Frontend UI)**
Create the "Виртуален килер" Vue interface. Use Pinia to manage the temporary UI state of the pantry while keeping it in sync with the Inertia-provided pantry data.


STEP 8, PHASE A: Virtual Pantry (Analysis & Plan)Architectural Approach (Pinia & Inertia Sync)
Target Files: resources/js/Pages/Pantry/Index.vue and a new Pinia store resources/js/stores/pantryStore.js.
Hydration Rule: Inertia is the primary source of truth. The Vue component will receive pantry_items via defineProps. 
You MUST hydrate the Pinia store with this data upon component mount. 
To prevent state mismatches, use a Vue watch on the Inertia props so that if the server data changes, the Pinia store updates automatically.  
UI Components & Interaction (Adding & Updating - RACE CONDITION PREVENTION)
Adding Items: Use a simple HTML <select> dropdown populated by the global ingredient dictionary to let users add new items to their pantry.  Inline Adjustments: For modifying quantities, implement simple inline + and - buttons next to each item.  Syncing Logic & Debounce (CRITICAL): Clicking + or - must update the Pinia state immediately for a snappy UI. However, you MUST debounce the subsequent Inertia request (e.g., using a 500ms delay via standard JavaScript timeout or Lodash). 
Rapid clicks should only result in a single database update once the user stops clicking to prevent backend race conditions and database locking. 
When it does fire, use { preserveState: true, preserveScroll: true } so the page does not flash.  
Data Display (Base Units & Grouping)Strict Base Units: Do NOT write frontend conversion logic. Display the quantity exactly as it is saved in the database (e.g., 1500 g, 500 ml). Keep the Vue component logic as simple and readable as possible.  Category Grouping: Group the rendered pantry items visually by their ingredient_categories (e.g., "Млечни", "Месо"). This utilizes the normalized database from Step 2 to keep the UI neat.  Localization (Bulgarian UI Rule)All user-facing text must be entirely in Bulgarian. Examples: "Виртуален килер" (Page Title), "Добави в килера" (Add button), "Празен килер" (Empty state).



**STEP 9: Polish - Shopping List Generator**
Add a feature to generate "Списък за пазаруване" from missing ingredients in a "Partial Match" recipe.


STEP 9, PHASE A: Shopping List Generator (Analysis & Plan)

Architectural Approach (The Generation Pipeline)

Target Files: app/Http/Controllers/ShoppingListController.php and a new Vue component resources/js/Pages/ShoppingList/Index.vue (or integrated into the sidebar as defined in Step 4).

Triggering Generation: On a "Partial Match" recipe UI, add a button to "Add missing ingredients to list". This will send an array of the missing ingredient IDs and their required base quantities to the backend.


Backend Storage: The controller will update or create records in the shopping_list_items table. If the item is already on the list but not purchased, add the new required quantity to the existing quantity.  

The Auto-Inject Transaction (Strict Backend Logic)

Action: When the user checks off an item as purchased in the UI, send an Inertia PATCH request to the backend.

Data Integrity Rule: The controller method handling this MUST wrap the logic in a DB::transaction().


Execution: 1. Update shopping_list_items setting is_purchased = true.
2. Query the user's pantry_items. If the ingredient_id exists, increment the quantity by the purchased amount. If it does not exist, create a new pantry_items record.  

This ensures the shopping list and virtual pantry remain in perfect, automated sync.

Frontend UI & Interaction (Minimalist UX)

Display: Render the list using simple checkboxes.

Visual Feedback: When is_purchased is true, apply a CSS line-through (line-through in Tailwind) and visually move it to a "Completed" section at the bottom of the list.

Smooth State: The Inertia request triggered by the checkbox must use { preserveState: true, preserveScroll: true } so the user can quickly check off multiple items without the page jumping.

Localization (Bulgarian UI Rule)

All UI elements must strictly be in Bulgarian.

Examples: "Списък за пазаруване" (Title), "Добави липсващите продукти" (Add button), "Купени" (Purchased section).



**STEP 10: Polish - Admin Panel**
Create a secure Vue view for Admins to manage users and moderate/standardize the Bulgarian ingredient database.


STEP 10, PHASE A: Admin Panel (Analysis & Plan)

Architectural Approach (Security & Routing)

Middleware: Create a dedicated EnsureUserIsAdmin middleware. This MUST check if auth()->user()->is_admin === true. Apply this middleware to a new group of /admin routes to strictly secure the backend endpoints.

Controllers: Create Admin/UserController and Admin/IngredientController to handle the specific management logic, adhering to the "Skinny Controllers" rule.

Feature 1: User Management (Basic CRUD)

Target File: resources/js/Pages/Admin/Users/Index.vue.

Functionality: Render a clean HTML table styled with standard Tailwind utility classes displaying all registered users (Name, Email, Registration Date, Role).

Actions: Provide simple buttons to "Promote to Admin" (toggle the is_admin boolean) and "Delete User". Wrap deletions in a standard Javascript confirm() dialog to prevent accidental clicks.

Feature 2: Ingredient Dictionary Moderation (Crucial for Algorithm Health)

Target File: resources/js/Pages/Admin/Ingredients/Index.vue.

Functionality: A table displaying the global ingredients table and their associated ingredient_categories.

Actions: Admins must be able to Add, Edit, and Delete master ingredients.

Constraint: Because the "Какво да сготвя?" algorithm relies on this base dictionary, include a warning or enforce constraints when deleting an ingredient that is heavily tied to existing recipes or pantries (e.g., rely on the database cascade rules established in Step 2, but provide a clean UI error message if a deletion is blocked).

UI Design & Localization (Bulgarian UI Rule)

Keep the design pragmatic. Use standard Breeze layout wrappers but perhaps add a distinct visual indicator (like a darker navigation bar) to show the user is in the "Admin Zone".

All text MUST be strictly in Bulgarian.

Examples: "Административен панел" (Admin Panel), "Управление на потребители" (Manage Users), "Речник на съставките" (Ingredient Dictionary), "Изтрий" (Delete).

**START COMMAND**
If you understand these rules, reply ONLY with: "Awaiting your command to begin Step 1, Phase A." Do not output any plans or code until I say "Go".